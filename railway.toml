[build]
builder = "nixpacks"

[deploy]
startCommand = "python weather_bot.py --interval 30"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
